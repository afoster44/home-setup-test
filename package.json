{
  "name": "test-stack",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:client && npm run setup:server",
    "setup:client": "cd test-stack.client && npm i",
    "setup:server": "cd test-stack.server && npm i",
    "start": "node test-stack.server/index.js"
  },
  "engines": {
    "node": "^14.15.4"
  }
}
